/* =========================================================================
   HCCS Config — GAS Endpoint
   ========================================================================= */
const CONFIG = {
    // Google Apps Script Web App URL — 배포 후 교체
    GAS_URL: ''
};
